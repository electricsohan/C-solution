Problem ID:-Sequence 3 #66

#include <stdio.h>
int main()
{
int i, j, n, k = 0;
scanf("%d",&n);
n=10-n;

for(i = 1; i <= n; ++i, k = 0)
  {
    for(j = 1; j <= n-i; ++j)
        {
        printf(" ");
        }

     while(k != i)
        {
         printf("%d ",10-i);
          ++k;
        }

      printf("\n");
          }

return 0;
}

Logic:-
Using 2 Nested For loop for traversing and printing numbers related to i,j. This code takes input(n) from User.
